---
import Layout from "../../layouts/Layout.astro";
import SwaggerViewer from "../../components/SwaggerViewer.tsx";
---

<Layout title="OpenAPI / Swagger 可视化编辑器">
    <div class="container mx-auto px-4 py-8 flex flex-col items-center">
        <div
            class="w-full max-w-7xl mb-8 flex flex-col md:flex-row md:items-center justify-between gap-4"
        >
            <div>
                <h1
                    class="text-3xl font-extrabold text-slate-900 dark:text-white mb-2 flex items-center gap-3"
                >
                    <span
                        class="bg-brand-100 dark:bg-brand-900/40 text-brand-600 dark:text-brand-400 p-2 rounded-xl text-2xl shadow-sm border border-brand-200 dark:border-brand-800/50"
                        >📊</span
                    >
                    <span
                        class="bg-clip-text text-transparent bg-gradient-to-r from-slate-900 via-brand-600 to-slate-900 dark:from-white dark:via-brand-400 dark:to-white"
                    >
                        OpenAPI 可视化编辑器
                    </span>
                </h1>
                <p
                    class="text-slate-600 dark:text-slate-400 text-sm md:text-base max-w-2xl leading-relaxed"
                >
                    导入或粘贴 OpenAPI / Swagger 配置文件（JSON 或 YAML
                    格式），即可实时生成交互式 API
                    接口文档，并支持在左侧面板便捷地手写修改并同步预览。
                </p>
            </div>

            <a
                href="/"
                class="self-start md:self-center shrink-0 flex items-center gap-2 px-4 py-2 text-sm font-medium text-slate-600 bg-slate-100 hover:bg-slate-200 dark:text-slate-300 dark:bg-slate-800 dark:hover:bg-slate-700 rounded-lg transition-colors shadow-sm"
            >
                <svg
                    xmlns="http://www.w3.org/2000/svg"
                    width="16"
                    height="16"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2"
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    ><path d="m15 18-6-6 6-6"></path></svg
                >
                返回工具导航
            </a>
        </div>

        <div
            class="w-full max-w-7xl animate-fade-in-up shadow-2xl rounded-2xl border border-slate-200 dark:border-slate-800"
        >
            <SwaggerViewer client:only="react" />
        </div>
    </div>
</Layout>

<style is:global>
    /* 我们可以对 Swagger UI 做一些微调以适配我们整体的排版，默认 Swagger UI 是浅色模式 */
    .swagger-ui-wrapper {
        background: transparent;
    }
    .swagger-ui-wrapper .swagger-ui {
        font-family:
            "Inter",
            system-ui,
            -apple-system,
            sans-serif;
    }
    .swagger-ui .info .title {
        color: #0f172a !important;
    }
</style>
