# Cloudflare Pages CI 构建须知

## 部署环境
- **平台**: Cloudflare Pages (Linux x64)
- **安装**: `npm ci`（严格模式，要求 package-lock.json 与 package.json 完全同步）

## Native 依赖兼容性规则

### ⚠️ 禁止写入 package.json 的包
以下平台特定的 native 绑定是 optional dependencies，由 npm 根据运行平台自动安装：

- `@rollup/rollup-darwin-arm64`
- `lightningcss-darwin-arm64`
- `@tailwindcss/oxide-darwin-arm64`

**写入 package.json 会导致 Cloudflare CI 失败**（平台不匹配 EBADPLATFORM）。

### 本地开发解决方案
如果 `npm install` 后本地构建报 `Cannot find module @rollup/rollup-darwin-arm64` 等错误：

```bash
npm install --no-save @rollup/rollup-darwin-arm64 lightningcss-darwin-arm64 @tailwindcss/oxide-darwin-arm64
```

`--no-save` 确保只安装到 node_modules，不写入 package.json。

### prebuild 脚本
`package.json` 中的 `prebuild` 脚本已处理 Linux x64 环境的 rollup 绑定：
```json
"prebuild": "node -e \"try{require.resolve('@rollup/rollup-linux-x64-gnu')}catch(e){process.exit(1)}\" || npm i @rollup/rollup-linux-x64-gnu --no-save || true"
```

## 添加新依赖时的检查清单
1. 确认新包不含仅限特定平台的 native 绑定
2. 运行 `npm install` 后检查 package.json 是否引入了不应有的平台包
3. 确认 `package-lock.json` 已更新并提交
4. 本地 `npm run build` 验证通过
